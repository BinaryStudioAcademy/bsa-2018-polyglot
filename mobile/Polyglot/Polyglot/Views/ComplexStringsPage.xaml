<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Polyglot.Views.ComplexStringsPage"
             Title="Complex Strings">
    <ListView 
            ItemsSource="{Binding ComplexStrings}"
            CachingStrategy="RecycleElement"
            RowHeight="70">

        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <StackLayout>
                        <Label HorizontalOptions="Start" Font="30,bold"  TextColor="Black" Text="{Binding Key}" />
                        <Label Text="{Binding OriginalValue}" />
                    </StackLayout>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>
