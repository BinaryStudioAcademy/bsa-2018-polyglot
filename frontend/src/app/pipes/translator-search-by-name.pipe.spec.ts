import { TranslatorSearchByNamePipe } from './translator-search-by-name.pipe';

describe('TranslatorSearchByNamePipe', () => {
  it('create an instance', () => {
    const pipe = new TranslatorSearchByNamePipe();
    expect(pipe).toBeTruthy();
  });
});
